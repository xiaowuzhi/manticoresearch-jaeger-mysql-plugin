#!/bin/bash

# ManticoreSearch 查询工具启动脚本
# 包含代理服务器，解决 CORS 问题

cd "$(dirname "$0")"

PORT=8080
URL="http://localhost:$PORT/manticore-query.html"

echo "╔══════════════════════════════════════════════════════════╗"
echo "║     ManticoreSearch 查询工具                              ║"
echo "╚══════════════════════════════════════════════════════════╝"
echo ""
echo "🚀 启动服务器..."
echo "📍 访问地址: $URL"
echo ""
echo "按 Ctrl+C 停止服务器"
echo ""

# 自动打开浏览器
(sleep 1 && open "$URL" 2>/dev/null || xdg-open "$URL" 2>/dev/null) &

# 启动代理服务器
python3 query-server.py
